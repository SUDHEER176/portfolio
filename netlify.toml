[build]
  base = "potfolio"
  command = "npm install --include=dev && npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  VITE_STATIC_EXPORT = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
